Everyone, the Day 1 contract baseline (schemas, examples, interfaces, architecture docs) is available. It is the starting point for this week's implementation work, not a claim that the Week 1 chains are complete.

**Schedule**: Six working days within the Jul 17–Jul 24 window. Mid-week check: Day 3. Final review: Day 6. Confirm the actual calendar invitations separately.

**Working repository**: Submit all Week 1 contracts, implementations, tests, documentation, and sanitized experiment records to `r1-embodied-os/`. Keep `ABot-Claw/` as a read-only reference checkout for studying existing services and execution patterns. No code in `r1-embodied-os/` should import from `ABot-Claw/`. Day 1: Xiong Yingjie will publish the exact revision and target branch for both repositories.

This week's shared goal is to complete three minimal chains:

1. State & telemetry (Matheus): mock data or confirmed R1 read-only data → Normalizer → Telemetry/RobotState → QA.
2. Minimal visual perception (Daniil): CameraFrameMetadata → MediaResolver (Mock) → Fake Detector → DetectionFrameResult → QA.
3. Minimal audio loop (Abdullokh): AudioChunkMetadata → AudioResolver (Mock) → Fake ASR → ASRResult → QA.

Li Jiahui is not running data chains this week — instead delivering the R1 capability baseline (SDK environment, sensor inventory, capability matrix), which is the factual foundation for the other three chains.

Shared implementation and ownership rules:

- Xiong Yingjie reviews and merges contract-shape changes under `contracts/`, `examples/`, and `interfaces/`. Workstream contributors implement those interfaces and propose contract changes through review rather than copying or independently redefining them.
- Reusable modules go under `implementations/telemetry/`, `implementations/perception/`, or `implementations/audio/`; `experiments/week01/` contains sanitized logs and results only.
- `TelemetryNormalizer.normalize` returns a complete `Telemetry` object for valid input. Invalid or missing required raw fields produce no partial Telemetry and raise `ValueError` with one stable reason-code prefix: `missing_required_field`, `invalid_field`, or `invalid_timestamp`.
- Day 3 freezes contract v0.1. Any later shape change must update the schema, Python type, examples, documentation, and tests together.

---

### Xiong Yingjie: Architecture, interface definition, QA & integration

#### First three days (Day 1–3)

1. Produce a one-page architecture overview covering the responsibilities and current scope of:

   - R1 Source / Robot Adapter seam
   - Edge Data Adapter
   - Perception Adapter
   - Contract Validation
   - Mock Data Source
2. Review existing `Telemetry`, `RobotState`, `CapabilityReport`, Camera/Audio Metadata contracts and examples. Also review the Python TypedDict and Protocol definitions under `r1-embodied-os/interfaces/` (`models.py`, `edge_interfaces.py`, `robot_adapter.py`, `week01_models.py`, `week01_interfaces.py`) to ensure B, C, and D's implementations align with existing interfaces.
3. Publish and verify the shared in-repository contract path so no one copies schemas or TypedDicts:

   - `r1-embodied-os/week01_contracts.py` is the single resolver for `contracts/`, `examples/`, and `interfaces/`; pytest activates it through the existing `tests/conftest.py`.
   - All teammates work from `r1-embodied-os/` and import the shared interfaces. Do not create a second contract-path helper under `ABot-Claw/` and do not copy contract files into implementation directories.
   - Use the existing `requirements-dev.txt`, which contains only the contract-test dependencies. No ROS, Torch, CUDA, RealSense, or robot SDK dependencies may be added to the Week 1 baseline.
   - Verify from `r1-embodied-os/`: `python -m pytest tests/` and `python -m tests.validate_all_contracts`.
4. Define the internal perception contract v0.1, split into outer and inner layers:

   Outer `DetectionFrameResult` (corresponds to YOLO's per-frame response):

   - `schema_version`
   - `source_frame` (constructed from `robot_id + sensor_id + sequence`, not a ROS TF frame_id)
   - `detector` (implementation source label: `fake` / `yolo_v5`)
   - `image_width` / `image_height`
   - `processed_at`
   - `detections: Detection2D[]` (empty array means detection succeeded but no targets found)

   Inner `Detection2D` (single detection result):

   - `detection_id`
   - `class_id` / `class_name`
   - `confidence` (range `[0, 1]`)
   - `bbox_xyxy` (four non-negative numbers, `x1 < x2`, `y1 < y2`, must not exceed image dimensions)
   - `observed_at`

   Additional rules:

   - CameraFrameMetadata's `width_px` / `height_px` can be null → when dimensions are missing, return `image_size_unavailable` and do not generate Detection2D results that claim verification.
   - `frame_id` is a media frame identifier, not a ROS/TF coordinate frame.
   - bbox ordering and out-of-bounds checks are semantic validation, not JSON Schema `minimum`/`maximum` constraints.
   - Include a mapping table from YOLO's existing fields to DetectionFrameResult + Detection2D.
5. For each object, document: producer, consumer, field constraints, error returns, and unknown-value handling. Produce corresponding Python TypedDicts. Also define `Protocol` signatures for the three chains so B, C, and D implement against a shared interface — at minimum: `TelemetryNormalizer`, `MediaResolver`, `DetectorBackend`, `ASRBackend`. Also define the complete `ASRResult` v0.1 as described in Abdullokh's section below.
6. Write an automated contract validation script:

   - Each schema validates against JSON Schema Draft 2020-12.
   - Valid examples pass.
   - Intentionally invalid examples fail as expected.
   - The validation report distinguishes four result categories: `schema_valid` / `schema_invalid` / `semantic_valid` / `semantic_invalid`.
   - `jsonschema` does not check `format: "date-time"` by default — use `FormatChecker` explicitly.
   - bbox ordering/out-of-bounds, expired commands, missing dimensions, etc. are semantic checks executed separately after schema validation passes.
   - `media_unavailable` is returned by MediaResolver — do not add extra fields to CameraFrameMetadata (schema enforces `additionalProperties: false`).
   - `capability unverified` comes from CapabilityReport, not from Telemetry/RobotState fields — the normalizer must not inject it into state JSON.
7. Write the integration acceptance scenarios to be executed on Day 6.

#### Last three days (Day 4–6)

1. Review documentation and code from A, B, C, and D. Check interface consistency and unverified assumptions.
2. Bring B's telemetry chain, C's visual perception chain, and D's audio perception chain into unified QA.
3. Execute automated tests. Record inputs, outputs, failure reasons, and reproduction commands.
4. Produce a summary of what passed, what failed, what remains unverified, and what requires teacher confirmation.
5. Deliver the Week 1 architecture and QA report.

#### Suggested deliverables

- `docs/week01/architecture_and_interfaces.md`
- `docs/week01/detection2d_contract.md`
- `contracts/week01/detection-frame-result.schema.json` (Draft 2020-12, `additionalProperties: false`)
- `contracts/week01/asr-result.schema.json` (Draft 2020-12, `additionalProperties: false`)
- `examples/week01/detection-frame-result.json`
- `examples/week01/asr-result.json`
- `interfaces/week01_models.py` (TypedDicts for DetectionFrameResult, Detection2D, ASRResult)
- `tests/validate_all_contracts.py`
- `tests/integration/test_scenarios.md`
- `experiments/week01/qa_architecture_log.md`
- `experiments/week01/integration_report.md`

#### Acceptance criteria

- Module responsibilities, data flows, and what is explicitly out of scope are clear.
- `Detection2D` producer, consumer, and exception handling are clearly described.
- All existing contracts and examples have reproducible validation results.
- At minimum covers: normal telemetry, unknown safety state, invalid timestamp, invalid bbox, image size mismatch, media reference unavailable, capability unverified.
- All four teammates' outputs have been reviewed; if no issues found, document the review scope and pass — no mandatory minimum comment count.

---

### Li Jiahui: R1 hardware, SDK, sensor & safety fact baseline

This week's goal is to confirm facts and constraints. No real robot motion control.

#### First three days (Day 1–3)

1. Record the actual hardware model, firmware version, SDK version, OS, build toolchain, and permission conditions.
2. Set up the SDK build environment using first-party materials. Record installation commands, compilation, output, errors, and resolution process.
3. Prioritize compiling the SDK or inspecting read-only examples. Do not run examples that could cause robot motion, sound output, or hardware actuation without lab safety approval.
4. Investigate and record whether evidence exists for the following capabilities:
   - RobotState/Telemetry readable fields
   - Camera
   - Depth
   - Microphone
   - Speaker
   - Timestamps and coordinate frames
5. Document the applicable physical E-Stop, power-on, remote takeover, and test approval procedures. Mark anything that cannot be confirmed as `unverified`.

#### Last three days (Day 4–6)

1. Produce the R1 capability matrix. Statuses are limited to: `supported`, `unavailable`, `unverified`, or `degraded`.
2. Each status entry must include: evidence source, version, verification date, and verification method.
3. Organize candidate mappings from R1 raw fields to `Telemetry`, `RobotState`, `CameraFrameMetadata`, and `AudioChunkMetadata`. For audio, particularly check `encoding`, `sample_rate_hz`, `channels`, `chunk_duration_ms`; mark as `unverified`/`null` if not confirmed.
4. Clearly distinguish: fields that are verified, fields that are only contract placeholders, and fields that are completely absent.
5. If the physical robot is unavailable, submit: build records, interface/header inventory, blocking reasons, and next-step verification plan.

#### Suggested deliverables

- `docs/week01/r1_sdk_baseline.md`
- `docs/week01/r1_sensor_capability_matrix.md`
- `docs/week01/r1_readonly_mapping.md`
- `experiments/week01/r1_hardware_log.md`
- `experiments/week01/r1_capability_report.json`

Do not overwrite normative examples with local environment or device evidence. Sanitized logs belong under `experiments/week01/`; credentials, private endpoints, local absolute paths, and unpublished sensitive information must not be committed anywhere.

#### Acceptance criteria

- Hardware, firmware, SDK, and sensor information all have cited sources. Where unavailable, clearly state the blocking reason.
- Every capability has evidence or is explicitly marked `unverified`. Note the schema's conditional constraint: `status: "supported"` requires non-empty `evidence` and a valid `verified_at` timestamp; `status: "unverified"` allows empty `evidence`. Do not fabricate evidence to fill the table.
- Do not infer robot capability from SDK symbols alone.
- Do not describe software stop methods as physical E-Stop.
- Whether or not the physical robot is available, produce complete, reproducible documentation.

---

### Matheus: R1 Read-only Data Adapter — Telemetry, RobotState & Camera Metadata

This week handles only reading, normalization, and validation. No SkillCommand, lease, action scheduling, or real robot safety control. This is NOT the full R1 Robot Adapter (which includes motion and action commands).

#### First three days (Day 1–3)

1. Implement `telemetry_normalizer`:
   - Input: Mock data with clearly labeled sources, or read-only fields confirmed by Li Jiahui.
   - Output: conforms to `telemetry.schema.json`.
   - Unknown safety state must remain `null` or the contract-allowed unknown expression — do not silently write `false`.
2. Implement `robot_state_provider`:
   - Maintain a current RobotState snapshot.
   - Correctly handle UTC timestamps and local monotonic time.
   - Clock source must be injectable for testing.
3. Prepare test data for: normal, missing fields, timestamp anomalies, and stale data. Note: capability status comes from CapabilityReport — do not inject capability fields into Telemetry or RobotState JSON.
4. Day 3: present normalization results with schema validation.

#### Last three days (Day 4–6)

1. Implement CameraFrameMetadata validator. Handle metadata and transport references only — no binary or Base64 media.
2. Supplement verified/unverified field mappings based on Li Jiahui's results. Do not fill in guessed specifications.
3. Provide Daniil with: one valid CameraFrameMetadata and one media_unavailable scenario.
4. Complete anomaly tests and data flow documentation. Execute read-only chain integration tests with Xiong Yingjie.

#### Suggested deliverables

- `implementations/telemetry/` (contains normalizer, state_provider, Camera metadata validator)
- `tests/telemetry/` (telemetry + Camera metadata scenarios)
- `docs/week01/r1_robot_adapter_notes.md`
- `experiments/week01/r1_robot_adapter_log.md`

#### Acceptance criteria

- Telemetry and RobotState outputs pass their corresponding schemas.
- Tests cover: normal, missing fields, unknown safety state, and stale data. Capability status belongs in CapabilityReport, not in Telemetry/RobotState — test that unverified capability keeps the pipeline in Mock-only mode without injecting capability status into state JSON.
- Camera metadata JSON contains only metadata and transport references.
- All modules import without networking, threads, device access, or SDK/DDS initialization.
- Code clearly distinguishes Mock data from real evidence at all times.

---

### Daniil: MediaResolver, Fake Detector & visual perception loop

Getting real YOLO running is optional. Fake Detector + interface closure is mandatory.

#### First three days (Day 1–3)

1. Read the existing YOLO service's input, output, and startup instructions (`service_layer/YOLO/SERVICE_AGENT.md`). Read `robot_layer/arm_piper/agent_server/robot_sdk/yolo_sdk.py` **only** for its YOLO return fields and post-processing approach as a reference — this file contains ROS, networking, threads, and 3D projection. **Do not treat it as this week's Adapter target implementation. Do not import or run it in tests.** Record dependencies and current blockers.
2. Implement `MediaResolver` (Mock version): input CameraFrameMetadata, output `media_available` status and fixture identifier. No real I/O, Base64 encoding/decoding, or file reads. `media_unavailable` is returned by this layer — do not add extra fields to metadata objects (schema enforces `additionalProperties: false`).
3. Based on the `DetectionFrameResult` + `Detection2D` v0.1 defined by Xiong Yingjie, implement Fake Detector:
   - Input: fixture identifiers from MediaResolver output, plus test-harness-managed fixtures.
   - Output: deterministic DetectionFrameResult JSON (containing a detections array).
   - Fake Detector and the optional YOLO Detector share the same `Detector` interface — they do not consume CameraFrameMetadata directly.
4. Explain how Fake Detector and YOLO Detector switch via the MediaResolver + Detector interface.
5. Prepare three scenarios: normal detection, empty detection, and invalid bbox.
6. Day 3: present MediaResolver → Fake Detector → DetectionFrameResult passing schema validation.

#### Last three days (Day 4–6)

1. Integrate the CameraFrameMetadata provided by Matheus. Handle media_unavailable scenarios through MediaResolver.
2. Implement or document the following exceptions: invalid_bbox / image_size_mismatch / media_unavailable / detector_unavailable.
3. If the YOLO environment can be started without blocking the main line, a YOLO Adapter may be added as a bonus. If it fails, submit the full commands and errors — no requirement to force a fix.
4. Do not commit model weights, raw images, videos, or outputs. Test resources follow repository rules.
5. Deliver visual perception data flow documentation and Fake/YOLO switching explanation.

#### Suggested deliverables

- `implementations/perception/` (contains MediaResolver + Fake Detector)
- `tests/perception/` (visual scenarios)
- `docs/week01/perception_adapter_notes.md`
- `experiments/week01/perception_log.md`

#### Acceptance criteria

- Fake Detector produces stable DetectionFrameResult output conforming to v0.1 (containing a detections array; empty array means no targets).
- Can explain input fields, output fields, and the Fake/YOLO switching seam.
- Normal and anomaly scenarios cover at minimum: normal detection, empty detection, invalid bbox, media_unavailable, detector_unavailable.
- Do not claim Camera, Depth, or YOLO availability as verified R1 capability unless Li Jiahui provides evidence.
- Can complete a visual Mock chain demo with Matheus's CameraFrameMetadata.

---

### Abdullokh: AudioResolver, Fake ASR & audio perception loop

This week implements only the Mock audio perception contract and processing chain. No real microphone connection, no audio playback, no real ASR/TTS service integration.

#### First three days (Day 1–3)

1. Read and verify the existing content under `r1-embodied-os/`: `contracts/audio-chunk-metadata.schema.json`, `examples/audio-chunk-metadata.json`, and the `audio_input` field in CapabilityReport.
2. Work with Xiong Yingjie to finalize `ASRResult` v0.1. At minimum include: `schema_version`, `result_id`, `source_audio` (identified by `robot_id + sensor_id + sequence`), `recognizer` (implementation source label: `fake` or future name), `status`, `text`, `language`, `confidence`, `processed_at`, `error`. `status` must distinguish at least: `succeeded` / `no_speech` / `media_unavailable` / `unsupported_format` / `recognizer_unavailable` / `failed`.
3. Implement `AudioChunkMetadataValidator`: validate schema and RFC 3339 timestamps, validate transport references and expiry. Do not read binary audio.
4. Implement Mock `AudioResolver`: input AudioChunkMetadata, output fixture identifier or a clear error. No file, network, device, or Base64 operations.
5. Implement deterministic `FakeASR`: same fixture always produces the same result. Support normal speech and `no_speech` scenarios.
6. Implement `ASRNormalizer`: unify the output format of FakeASR and future real ASR, producing output conforming to the `ASRResult` schema.
7. Day 3: present AudioChunkMetadata → AudioResolver → FakeASR → ASRResult passing schema validation.

#### Last three days (Day 4–6)

1. Complete all scenarios: normal transcription, `no_speech`, invalid metadata, expired transport, `media_unavailable`, `unsupported_format`, `recognizer_unavailable`, confidence out of range (8 categories total).
2. Define the two interfaces: `AudioResolver.resolve(metadata) → AudioHandle / ResolutionError` and `ASRBackend.transcribe(audio_handle) → RawASRResult`.
3. Document which Adapter to replace when connecting a real ASR in the future. Do not select or connect to a specific cloud ASR this week.
4. Complete schema, semantic, and integration tests with Xiong Yingjie.
5. Based on Li Jiahui's investigation results, mark R1 audio capability as verified/unverified. Without evidence, only refer to it as a Mock chain.

#### Suggested deliverables

- `implementations/audio/` (contains validator, AudioResolver, FakeASR, ASRNormalizer)
- `tests/audio/` (8 scenario categories)
- `docs/week01/audio_adapter_notes.md`
- `docs/week01/asr_result_contract.md`
- `experiments/week01/audio_adapter_log.md`

#### Acceptance criteria

- AudioChunkMetadata and ASRResult examples pass their corresponding schemas.
- FakeASR produces repeatable results for the same fixture.
- At minimum the 8 normal/anomaly scenario categories above are completed.
- `no_speech` is distinguishable from system failure in `status`.
- All modules import without networking, threads, file access, or device access.
- JSON contains no audio bytes, Base64, local paths, or private addresses.
- Do not claim R1 real microphone capability or audio format is verified.
- Can independently demo the complete Mock audio chain on Day 6.

---

## Meeting schedule

### Day 3: mid-week check & interface freeze (recommended 50 min)

Everyone must show existing files, test results, or failure records — no verbal-only reports.

1. Five people × 5 min each presenting current output (25 min total).
2. 15 min: confirm DetectionFrameResult/Detection2D v0.1, ASRResult v0.1, AudioChunkMetadata, AudioResolver (`AudioHandle` / `ResolutionError`), ASRBackend (`RawASRResult`), CameraFrameMetadata usage, and Mock data formats. Confirm the B (telemetry), C (visual perception), and D (audio perception) interface handoff points.
3. 10 min: resolve blockers — record who is responsible, what input is needed, and the resolution deadline.
4. Based on Li Jiahui's evidence, decide whether to attempt real camera or YOLO in the last three days. Without evidence, stay on the Mock main line.

Meeting must produce: meeting notes, interface version, change items, blocker list, and adjusted Day 4–6 plan.

### Day 6: read-only chain demo & weekly review (recommended 75 min)

1. Matheus demos: Mock/confirmed state → Normalizer → Telemetry/RobotState → QA.
2. Daniil demos: CameraFrameMetadata → MediaResolver → Fake Detector → DetectionFrameResult → QA.
3. Abdullokh demos: AudioChunkMetadata → AudioResolver → Fake ASR → ASRResult → QA.
4. Present the R1 capability matrix and read-only field mappings.
5. Run the key tests maintained by Xiong Yingjie live.
6. Summarize: what passed, what failed, what is unverified, and what needs teacher confirmation.
7. Only content that has passed review and testing and contains no credentials or sensitive information may be merged to the target branch.

---

## Shared completion criteria

A task is complete only when all of the following are met:

- Artifacts committed to the agreed branch and paths.
- Reproduction commands, inputs, and expected outputs are documented.
- Test results or complete failure records exist.
- Documentation distinguishes verified facts, design decisions, and unverified questions.
- No passwords, tokens, private addresses, personal information, large model weights, raw audio/video, or local absolute paths.
- Blocked tasks must document: what was attempted, the full error, current assessment, what help is needed, and next steps.

## Daily async update format

```text
Done: files/tests completed today and ready to review
Next: what you plan to complete tomorrow
Blocked: the specific blocker and what you've already tried
Need: what you need, from whom, by when
```

## Explicitly out of scope this week

- No unapproved robot motion, sound output, or hardware actuation.
- No real R1 Adapter, SkillCommand runtime, lease manager, real Safety Supervisor, or physical E-Stop implementation.
- Do not assume R1 supports depth camera, navigation, obstacle avoidance, `sit`, ROS 2, or other unverified capabilities.
- Do not connect to real ASR/TTS engines or audio storage (the Fake ASR→ASRResult loop is the only exception). Do not simultaneously expand into Memory, Critic, VLA, complex grasping, or multi-robot coordination.
- Do not commit internal test images, model weights, raw media, credentials, private endpoints, local absolute paths, or unpublished sensitive device information to `r1-embodied-os/`.

Next week, based on this week's evidence, we will select one action-direction vertical slice: Agent Tool → SkillCommand → Mock Edge → Mock Robot Adapter → ActionStatus. Until the read-only data chains and capability facts are stable, there is no rush to connect real robot motion.
